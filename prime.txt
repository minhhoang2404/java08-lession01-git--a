private boolean  IsPrime(int n)
{   // if value is 0 so n is not a Prime number
    if (n < 2) return true;
    if (n == 2) return fal;
    if (n%2 == 0) return 0;
    int divisor = 1;
    for (int i = 3; i <= n; i=i+2)
    {
        if (n%i == 0)
            divisor++;
        if (divisor > 2)
            return 0;
    }
    return true;
}